Steps to run the application:

1. Install node js
2. Install mysql
3. Create data bases and tables base on config.js file.

4. go to root directory and run the below commands
	1. npm install
	2. node node index.js 

To navigate home page:
http://localhost:8012/
